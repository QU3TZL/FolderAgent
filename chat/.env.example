# Environment Configuration Template

# Service URLs
NEXT_PUBLIC_UPGRADE_URL=http://localhost:8000          # UpGrade frontend URL
NEXT_PUBLIC_UPGRADE_API_URL=http://localhost:8000      # UpGrade API URL
VECTORIA_INTERNAL_URL=http://localhost:3000            # Vectoria service URL (local) or internal Render URL (prod)

# Base Path Configuration
BASE_PATH=/chat                                        # Base path for the chat interface

# Port Configuration
PORT=3000                                             # Use 3000 for local dev, 10000 for Render deployment

# Node Environment
NODE_ENV=development                                  # Use 'development' for local, 'production' for deployment

# Optional: Node Memory Settings (for production)
# NODE_OPTIONS=--max_old_space_size=4096             # Uncomment in production if needed

# Notes:
# - For production deployment on Render.com:
#   - Set PORT to 10000
#   - Use Render's internal URL for VECTORIA_INTERNAL_URL (e.g., vectoria-xxxx:3000)
#   - Set NEXT_PUBLIC_UPGRADE_URL to https://tryupgrade.live
# - For local development:
#   - Use localhost URLs
#   - Ensure Vectoria is running locally on port 3000 